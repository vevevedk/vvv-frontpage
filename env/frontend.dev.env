# Frontend (Development)
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:8001/api
DJANGO_API_URL=http://backend:8000/api

# Optional local DB vars for any Next API routes
DB_HOST=postgres
DB_PORT=5432
DB_NAME=vvv_database_dev
DB_USER=vvv_user
DB_PASSWORD=dev_password_change_me

# JWT secret for Next API routes (if used)
JWT_SECRET=dev-next-secret-change-me

# Lead notifications (veveve.dk)
LEAD_NOTIFY_EMAILS=andreas@veveve.dk,anders@veveve.dk
# Slack webhook is a secret. Set it via `.env.local` or shell env (not committed).
SLACK_LEADS_WEBHOOK_URL=

















